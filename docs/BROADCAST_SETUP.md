# Функционал рассылки (Broadcast) - Инструкция по настройке

## Обзор

Функционал массовой рассылки позволяет администраторам отправлять сообщения пользователям бота с гибкой фильтрацией и возможностью отложенной отправки.

## Возможности

### 📊 Фильтрация пользователей

- **Все пользователи** - рассылка всем зарегистрированным пользователям
- **С активными ключами** - только пользователи с действующими VPN-ключами
- **С истекшими ключами** - пользователи с просроченными ключами
- **Без ключей** - пользователи, которые никогда не покупали ключи
- **Платящие пользователи** - пользователи с хотя бы одной успешной покупкой
- **Бесплатные пользователи** - пользователи без покупок
- **По языку** - фильтр по языку интерфейса (ru/en)
- **Новые пользователи** - зарегистрированные за последние 7 дней
- **Старые пользователи** - зарегистрированные более 30 дней назад

### ⏰ Отложенная отправка

- Возможность запланировать рассылку на конкретное время
- Автоматическая обработка запланированных рассылок каждую минуту
- Поддержка часовых поясов (UTC по умолчанию)

### 📈 Мониторинг и статистика

- Отслеживание статуса рассылки (pending, in_progress, completed, failed)
- Счётчики успешно отправленных и неудачных сообщений
- История всех рассылок
- Детальная информация по каждой рассылке

## Установка и настройка

### 1. Выполните миграцию базы данных

Запустите SQL-скрипт для создания необходимых таблиц:

```bash
# Для Supabase - выполните в SQL Editor
cat migrations/add_broadcast_messages.sql
```

Или скопируйте содержимое файла `migrations/add_broadcast_messages.sql` в Supabase SQL Editor и выполните.

Скрипт создаст:
- Таблицу `broadcast_messages` - хранение рассылок
- Таблицу `broadcast_recipients` - отслеживание получателей
- Необходимые индексы для оптимизации запросов

### 2. Проверьте установку зависимостей

Убедитесь, что установлен пакет `node-cron`:

```bash
npm install node-cron
```

### 3. Перезапустите бота

```bash
npm run dev
# или
npm start
```

При запуске вы должны увидеть сообщение:
```
✅ Запланированные рассылки - каждую минуту
```

## Использование

### Создание новой рассылки

1. Откройте бот и перейдите в **Административную панель**
2. Нажмите кнопку **📢 Рассылка**
3. Выберите **📤 Новая рассылка**
4. Выберите нужный фильтр пользователей
5. Введите текст сообщения (поддерживается HTML-разметка)
6. Подтвердите отправку

### HTML-форматирование

Вы можете использовать следующие HTML-теги в сообщениях:

```html
<b>Жирный текст</b>
<i>Курсив</i>
<u>Подчёркнутый</u>
<code>Моноширинный текст</code>
<a href="https://example.com">Ссылка</a>
```

Пример сообщения:
```html
<b>🎉 Специальное предложение!</b>

Только сегодня скидка 50% на все тарифы!

<i>Предложение действует до 23:59</i>

<a href="https://t.me/your_bot">Купить ключ</a>
```

### Отложенная рассылка

⚠️ Функция находится в разработке. Для её активации необходимо:

1. Добавить обработчик `broadcast_schedule` в `BroadcastCallbacks.js`
2. Реализовать парсинг времени из текстового сообщения
3. Добавить валидацию и форматирование даты/времени

### Просмотр истории

1. Откройте **Административная панель** → **📢 Рассылка**
2. Нажмите **📜 История**
3. Вы увидите последние 10 рассылок с их статусами

## Архитектура

### Компоненты системы

#### 1. BroadcastService (`src/services/BroadcastService.js`)
- Основной сервис для управления рассылками
- Создание и запуск рассылок
- Асинхронная отправка сообщений порциями
- Обработка ошибок и retry логика

#### 2. BroadcastCallbacks (`src/bot/handlers/callbacks/BroadcastCallbacks.js`)
- Обработчик пользовательского интерфейса
- Многошаговый процесс создания рассылки
- Хранение состояния в памяти (sessions)

#### 3. BroadcastMessages (`src/services/messages/BroadcastMessages.js`)
- Шаблоны сообщений для UI
- Форматирование статусов и информации о рассылках

#### 4. BroadcastScheduler (`src/schedulers/BroadcastScheduler.js`)
- Cron-задача для обработки запланированных рассылок
- Выполняется каждую минуту
- Проверяет наличие готовых к отправке рассылок

#### 5. Database Methods (`src/models/SupabaseDatabase.js`)
- `createBroadcast()` - создание рассылки
- `getBroadcastRecipients()` - получение списка получателей по фильтру
- `addBroadcastRecipients()` - добавление получателей
- `updateBroadcastStatus()` - обновление статуса
- `getPendingRecipients()` - получение следующей порции получателей
- `updateRecipientStatus()` - обновление статуса отправки
- И другие...

### Схема работы

```
Админ → BroadcastCallbacks → BroadcastService → Database
                                     ↓
                              Async Sending
                                     ↓
                              Update Status
```

### Ограничения отправки

- **Batch size**: 30 сообщений за раз
- **Delay**: 100мс между сообщениями
- **Retry**: Автоматическая обработка ошибок с логированием

Это предотвращает блокировку бота Telegram за спам.

## Локализация

Все тексты поддерживают два языка:
- Русский (`src/locales/ru/message.json`)
- Английский (`src/locales/en/message.json`)

Ключи переводов находятся в секции `admin.broadcast`.

## Troubleshooting

### Рассылка не запускается

1. Проверьте логи бота на наличие ошибок
2. Убедитесь, что таблицы созданы в базе данных
3. Проверьте, что планировщик запущен (должно быть сообщение при старте бота)

### Сообщения не отправляются некоторым пользователям

1. Проверьте таблицу `broadcast_recipients` на наличие ошибок
2. Пользователи могли заблокировать бота - это нормально
3. Проверьте поле `error_message` в таблице для деталей

### Запланированные рассылки не отправляются

1. Проверьте, что планировщик активен
2. Убедитесь, что время указано в правильном формате (UTC)
3. Проверьте статус рассылки в таблице `broadcast_messages`

## Безопасность

⚠️ **Важно:**
- Доступ к рассылкам имеют только администраторы из `ADMIN_IDS`
- Перед запуском массовой рассылки тестируйте её на небольшой группе
- Избегайте отправки спама - это может привести к блокировке бота
- Соблюдайте правила Telegram по массовым рассылкам

## Будущие улучшения

- [ ] Отложенная отправка через UI (сейчас в разработке)
- [ ] Поддержка медиа (изображения, видео)
- [ ] Шаблоны сообщений
- [ ] A/B тестирование
- [ ] Детальная аналитика (CTR, конверсия)
- [ ] Отмена рассылки в процессе выполнения
- [ ] Экспорт статистики рассылок

## Поддержка

Если у вас возникли вопросы или проблемы, создайте issue в репозитории проекта.
