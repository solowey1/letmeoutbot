# Бот поддержки

Бот поддержки - это отдельный Telegram бот для обработки запросов пользователей в службу поддержки.

## Возможности

- 📩 Пользователи могут отправлять вопросы в бот поддержки
- 🔔 Администраторы получают уведомления о новых сообщениях
- 💬 Админы могут отвечать на вопросы через кнопку "Ответить"
- 📝 Все сообщения сохраняются в базе данных
- 👥 Несколько админов могут работать одновременно

## Настройка

### 1. Создайте нового бота в Telegram

1. Найдите @BotFather в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и получите токен
4. Добавьте токен в `.env`:

```env
SUPPORT_BOT_TOKEN=your_support_bot_token_here
```

### 2. Запустите миграцию БД

Выполните SQL миграцию в вашей Supabase/PostgreSQL базе данных:

```bash
# Файл: migrations/add_support_tables.sql
```

Или через Supabase Dashboard:
- Перейдите в SQL Editor
- Вставьте содержимое `migrations/add_support_tables.sql`
- Нажмите "Run"

### 3. Запустите Support Bot

```bash
# Для разработки
npm run support:dev

# Для продакшена
npm run support:start
```

Или напрямую:

```bash
node support.js
```

## Использование

### Для пользователей

1. Откройте бота поддержки
2. Напишите ваш вопрос
3. Получите подтверждение
4. Ожидайте ответ от поддержки

### Для администраторов

1. Получите уведомление о новом вопросе
2. Нажмите кнопку "Ответить" под сообщением
3. Напишите ответ пользователю
4. Ответ будет отправлен автоматически

## Структура БД

### Таблица `support_messages`

Хранит все сообщения (вопросы пользователей и ответы админов):

- `id` - ID сообщения
- `user_telegram_id` - Telegram ID пользователя
- `user_first_name` - Имя пользователя
- `user_username` - Username пользователя
- `message_text` - Текст сообщения
- `message_type` - Тип: `user_question` или `admin_reply`
- `replied_by_admin_id` - ID админа, который ответил
- `replied_at` - Время ответа
- `created_at` - Время создания

### Таблица `admin_reply_state`

Хранит текущее состояние админа (кому он сейчас отвечает):

- `admin_telegram_id` - Telegram ID админа
- `replying_to_user_id` - ID пользователя, которому отвечает
- `replying_to_message_id` - ID сообщения
- `created_at` - Время начала ответа

## Запуск в продакшене

### С PM2

```bash
pm2 start support.js --name "support-bot"
```

### С systemd

Создайте файл `/etc/systemd/system/support-bot.service`:

```ini
[Unit]
Description=Support Bot
After=network.target

[Service]
Type=simple
User=your-user
WorkingDirectory=/path/to/letmeout
ExecStart=/usr/bin/node /path/to/letmeout/support.js
Restart=on-failure

[Install]
WantedBy=multi-user.target
```

Затем:

```bash
sudo systemctl enable support-bot
sudo systemctl start support-bot
```

## Безопасность

- ✅ Только админы из `ADMIN_IDS` могут видеть и отвечать на сообщения
- ✅ Все сообщения логируются в БД
- ✅ Каждый админ работает в своем контексте (не мешают друг другу)

## Расширение функционала

Вы можете легко добавить:

- Историю переписки для каждого пользователя
- Статистику по обращениям
- Категории вопросов
- Автоответы на частые вопросы
- Уведомления в основном боте о новых ответах

## Troubleshooting

### Бот не запускается

- Проверьте, что `SUPPORT_BOT_TOKEN` указан в `.env`
- Убедитесь, что токен валиден
- Проверьте подключение к базе данных

### Админы не получают уведомления

- Проверьте, что `ADMIN_IDS` правильно указаны в `.env`
- Убедитесь, что админы запустили бота (`/start`)

### Ответы не отправляются

- Убедитесь, что пользователь не заблокировал бота
- Проверьте логи на наличие ошибок
- Убедитесь, что таблицы БД созданы корректно
